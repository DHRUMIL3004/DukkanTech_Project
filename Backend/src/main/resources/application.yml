spring:
  config:
    import: optional:application-secret.yml

  datasource:
    url: jdbc:mysql://localhost:3306/billing_app
    username: root
    password: root

  mail:
    host: smtp.gmail.com
    port: 587
    username: abcd787465@gmail.com
    password: "nspl sdbf zijf hwyy"   # ✅ MUST be in quotes (has spaces)
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: false   # ✅ important fix

  main:
    banner-mode: off

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

twilio:
  account-sid: ACb45886204f9f70d575362ca0b6edfafa
  auth-token: 61a15bdf41f4737d5cf1a5f7eefa1452
  phone-number: 7874658874

server:
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    org.springframework: WARN
    org.hibernate: WARN
    org.eclipse.angus.mail: DEBUG
    jakarta.mail: DEBUG
    com.intech.dukaantech: DEBUG