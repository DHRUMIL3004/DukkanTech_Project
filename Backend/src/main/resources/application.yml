spring:
  datasource:
    url: jdbc:mysql://localhost:3306/billing_app
    username: root
    password: root

  main:
    banner-mode: off

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  servlet:
    context-path: /api