spring:
  config:
    import: optional:application-secret.yml

  datasource:
    url: jdbc:mysql://localhost:3306/billing_app
    username: root
    password: root

  mail:
    host: smtp.gmail.com
    port: 587
    username: abcd787465@gmail.com
    password: xegz mqtf hnrx owjs
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: true
            trust: smtp.gmail.com
            protocols: TLSv1.2

  main:
    banner-mode: off

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    org.springframework: WARN
    org.hibernate: WARN
    org.eclipse.angus.mail: DEBUG
    jakarta.mail: DEBUG
    com.intech.dukaantech: DEBUG