spring:
  datasource:
    url: jdbc:mysql://localhost:3306/billing_app
    username: root
    password: root
  security:
    user:
      name: akshay
      password: akshay

  jpa:
    hibernate:
      ddl-auto: update

server:
  servlet:
    context.path: /api

